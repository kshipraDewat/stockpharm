{
  "name": "stockist",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w @stockist/client\" \"npm run dev -w @stockist/server\"",
    "build": "npm run build -w @stockist/server && npm run build -w @stockist/client",
    "db:migrate": "npm run db:migrate -w @stockist/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
