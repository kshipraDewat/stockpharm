# Original Sibling Repositories (preserved)

These folders are **unchanged** at the parent `stockpharma/` level. The unified app in `../app/` is the merged implementation; these are reference sources.

| Code | Path from repo root | Stack | Standalone dev |
|------|---------------------|-------|----------------|
| **ERP** | `../stockpharmaerp` | Supabase + Vite | `npm run dev` (port 8080) |
| **HUB** | `../digi-swasthya-hub` | Supabase + Vite | `npm run dev` |
| **MED** | `../greetings-pal-git` | Supabase + Vite | `npm run dev` |
| **MR** | `../stockistpayments` | Supabase + Vite | `npm run dev` |
| **MVP** | `../digimvplaunch` | Client-only prototype | `npm run dev` |
| **DSW** | `../digiswasthya` | Client-only prototype | `npm run dev` |
| **DMVP** | `../digiswasthyamvp` | Mock Supabase | `npm run dev` |
| **SP** | `../STOCKIST-PHARMACY` | Express + PGlite (pre-unified copy) | `npm run dev` |

Each repo has its own `FEATURES.md` with exhaustive code-derived documentation.

**Do not delete these** — they are the source of truth for porting behavior into the unified app.
